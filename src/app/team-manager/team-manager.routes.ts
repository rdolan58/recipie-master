import { Route } from '@angular/router';
import { TeamManagementComponent } from './team-manager.component';

export const TEAM_MANAGER_ROUTE: Route[] = [
  {
    path: '',
    component: TeamManagementComponent,
  }
];

