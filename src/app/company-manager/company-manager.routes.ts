import { Route } from '@angular/router';
import { CompanyManagerComponent } from './company-manager.component';

export const COMPANY_MANAGER_ROUTE: Route[] = [
  {
    path: '',
    component: CompanyManagerComponent,
  }
];

